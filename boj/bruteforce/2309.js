// 일곱난장이
// https://www.acmicpc.net/problem/2309

// /dev/stdin
let fs = require('fs');
let [NM, ...BOARDS] = fs.readFileSync('예제.txt').toString().trim().split('\n');

NM = NM.split(' ');
let N = Number(NM[0]);
let M = Number(Nm[1]);

const solution = (list) => {};

solution(list);
